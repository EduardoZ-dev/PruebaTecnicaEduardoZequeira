using RouletteTechTest.API.Models.Entities;

namespace RouletteTechTest.API.Services.Interfaces
{
    /*public interface IRouletteRepository
    {
        // Sesiones
        Task<Session> GetSessionAsync(Guid sessionId);
        Task AddSessionAsync(Session session);

        // Rondas
        Task<Round> GetCurrentRoundAsync(Guid sessionId);
        Task AddRoundAsync(Round round);
        Task UpdateRoundAsync(Round round);

        // Operaciones transaccionales
        Task SaveChangesAsync();
    }*/
}
